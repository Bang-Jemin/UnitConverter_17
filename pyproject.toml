[project]
name = "unit-converter"
version = "0.0.0"
description = "Length unit conversion CLI (UnitConverter_17 harness)"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
